<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" /> <!--Uygulamanın, cihazdaki medya dosyalarındaki resimlere (fotoğraflara) okuma izni verir.-->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" /> <!--Uygulamanın, cihazdaki medya dosyalarındaki sesli medya (müzikler ve diğer ses dosyaları) için okuma izni verir.-->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> <!--cihazın harici depolama alanında (örneğin USB belleği) dosya okuma ve yazma izinlerini verir. -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" /> <!--Uygulamanın, cihazın harici depolama alanındaki dosyaları okuma izni verir.-->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_kare"
        android:label="KitapTuru"
        android:roundIcon="@mipmap/ic_launcher_daire"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication6"
        tools:targetApi="31">


        <activity
            android:name=".LoginActivity"
            android:exported="false" />
        <activity
            android:name=".RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".SearchActivity"
            android:exported="false" />
        <activity
            android:name=".GuncelleActivity"
            android:exported="false" />
        <activity
            android:name=".DetayActivity"
            android:exported="false" />
        <activity
            android:name=".AddBookActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="false">

        </activity>
        <activity
            android:name=".WelcomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>

